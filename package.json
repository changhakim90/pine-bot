{
  "name": "pine-bot",
  "version": "6.94.2",
  "description": "Autonomous player for Pine & Co (pineandco.online) — userscript built from src/, tested headless, runnable via Violentmonkey/Tampermonkey or Playwright.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "check": "node --check dist/pine-bot.user.js",
    "test": "npm run build && npm run check && node test/run.js",
    "run": "node run/playwright.js",
    "run:headless": "node run/playwright.js --headless"
  },
  "pineBot": {
    "rawBase": "https://raw.githubusercontent.com/changhakim90/pine-bot/main"
  },
  "devDependencies": {},
  "optionalDependencies": {
    "playwright": "^1.45.0"
  },
  "license": "MIT"
}
